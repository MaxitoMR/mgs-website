apiVersion: v1
kind: Secret
metadata:
  name: mgs-secrets
  namespace: mgs-app
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  REDIS_URL: "REPLACE_ME"
  SENTRY_DSN: "REPLACE_ME"
  AZURE_STORAGE_CONNECTION_STRING: "REPLACE_ME"
  PAGERDUTY_ROUTING_KEY: "REPLACE_ME"
  INCIDENT_IO_WEBHOOK_SECRET: "REPLACE_ME"
